# Placeholder values — replace before applying, or manage with your secret
# tooling of choice (sealed-secrets, external-secrets, SOPS, ...).
apiVersion: v1
kind: Secret
metadata:
  name: app-foundry-secrets
  namespace: app-foundry
type: Opaque
stringData:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: change-me
  POSTGRES_DB: app_foundry
  DATABASE_URL: postgresql+asyncpg://postgres:change-me@postgres:5432/app_foundry
  RESEND_API_KEY: ""
